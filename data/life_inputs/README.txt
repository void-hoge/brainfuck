Sample initial boards for `data/life.txt`.

Format:
- Exactly 20 lines.
- Each line has 20 characters.
- `#` means alive, `.` means dead.

Run example:
- `cat data/life_inputs/glider.txt | python3 -m bfcc.interpreter data/life.bf`

Controls:
- Press Enter to advance one generation.
